[profile.default]
via_ir = true
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 1
fs_permissions = [{ access = "read-write", path = "./" }]

# IMPORTANT: disable forge-lint notes/warnings during merge

